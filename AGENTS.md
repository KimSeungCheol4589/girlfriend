# 프로젝트 협업 규칙

## 공통 기준

- 작업 전에 PROJECT_PLAN.md, TECH_STACK.md, DESIGN.md, TASKS.md를 읽는다.
- 사용자 요청이 최우선이며, 현재 MVP 범위를 넘는 기능은 총괄과 조율한다.
- Codex 총괄은 작업 배분·설계 결정·검증·통합을 담당한다. 구현과 코드 검토는 이 PC의 Claude Code CLI에 맡긴다.
- Codex 담당 작업은 요구사항·파일 범위·완료 기준을 Claude에 전달하고 실행 상태·결과를 관리한다. Claude 호출이 막혀도 제품 구현을 Codex가 임의로 대신하지 않는다.
- 구현 후 별도의 새 Claude 호출로 커밋 차이를 검토한다. 구현 세션의 자기 평가를 독립 검토로 취급하지 않는다.
- 다른 작업의 대화가 자동 공유된다고 가정하지 않는다. 명시된 계약과 저장소 문서로 협업한다.

## 작업 위치와 소유권

- 각 작업은 자신의 Worktree와 기능 브랜치에서만 코드를 변경한다.
- 명령 실행 전 현재 경로, git status, 현재 브랜치를 확인한다. 다른 작업 폴더에서 checkout·reset·clean·commit하지 않는다.
- 총괄은 통합용 checkout에서 dev를 관리한다. 모든 개발과 기능 검증은 기능 브랜치 → dev 순서로 진행한다.
- 기능 브랜치는 최신 dev에서 시작하고 PR 대상은 dev다. 담당자는 dev·main에 직접 push·merge하지 않는다.
- main은 확정본이다. dev 검증 후 사용자가 반영을 결정한 범위만 총괄이 main으로 승격한다. 자동화는 main에 자동 병합하지 않는다.
- UI 담당 작업이 호출하는 Claude: 홈·추억·꾸미기 화면과 UI 컴포넌트. 첫 UI-001에 한해 앱 초기 설정 파일 생성까지 담당한다.
- DB 담당 작업이 호출하는 Claude: supabase/migrations, supabase/tests, DB 문서. 인증 UI와 공통 앱 설정은 별도 배정 전 수정하지 않는다.
- 총괄: TASKS.md, 공통 설계 문서, 통합 시 공통 설정·의존성, 미배정 기능의 후속 배분.
- 파일 소유권 변경은 총괄이 명시하고 TASKS.md를 갱신한다. 공통 파일 동시 수정은 피한다.

## 인터페이스와 범위

- DESIGN.md의 테이블·서버 작업 계약을 따른다. 변경 제안은 담당 보고서에 이유·영향을 남긴다.
- 개발용 더미 데이터는 실제 저장·인증 구현과 분리한다. 더미 동작을 실제 저장 성공으로 설명하지 않는다.
- 비밀 키, 실제 사진·이메일·DB 덤프를 저장소·로그·작업 보고서에 넣지 않는다.
- 필요한 외부 서비스 설정이나 자격 증명이 없으면 해당 검증만 미실행으로 명시하고 독립적으로 가능한 구현을 계속한다.
- 배포·운영 데이터 변경은 코드 통합과 별개다. 이번 협업 범위는 MVP 개발·테스트·저장소 통합이다.

## 완료 보고와 검토

1. 담당 브랜치에 변경을 커밋한다. 자신의 변경만 stage하고 force push하지 않는다.
2. 가능하면 기능 브랜치를 push한다. PR을 만들었다면 현재 작업에 첨부한다. PR 생성 자체는 필수 완료 조건이 아니다.
3. docs/handoffs/<작업ID>.md에 변경 요약, 변경 파일, 실행한 검증과 결과, 미실행 검증, 잔여 문제, 후속 작업을 기록한다. Claude 구현·검토의 실행 여부와 검토 대상 SHA도 남긴다.
4. 최종 응답에는 작업 ID, 브랜치, 최종 커밋 SHA, 보고서 경로, 통합 준비 여부를 남긴다. 커밋 SHA는 커밋 후 응답에 기록해 자기 참조를 피한다.
5. 검토 결과 수정 요청은 같은 작업·브랜치에서 처리한다. 완료 후 새 작업을 임의로 시작하지 않는다.

총괄은 완료 응답만으로 통합하지 않는다. 커밋 차이와 담당 범위를 검토하고 관련 검증을 수행한다. 통합 시점 커밋을 기록하고, 검증을 통과한 변경만 dev에 반영한다. main 승격 여부와 결정 근거는 별도 기록한다. 도구·환경 부족으로 필수 검증이 불가능하면 검토 대기로 유지한다.

## Claude 실행

- 2026-09-19 사용자 승인으로 로컬 Claude Code 신규 구현 세션과 별도 독립 검토 세션을 사용한다. 총괄이 작업을 배정하고 각 Codex 담당은 자기 Worktree에서 Claude 실행·인수인계·검증을 관리한다. 기존 클라우드 세션의 중복 구현 배정은 철회했다. 2026-09-21 사용자 재지시로 총괄은 지정 세션 `session_015jpN88JrPrHTJLvCa8iUdq`에 현재 `dev`의 읽기 전용 통합 사전 검토를 별도 배정했다. 전송 성공을 실행·완료로 취급하지 않고, 같은 작업·SHA를 중복 전송하지 않는다. 이 검토는 기능 브랜치 최종 SHA에 대한 별도 새 Claude 독립 검토를 대체하지 않는다.
- 구현 세션의 실행 ID·상태·결과를 직접 수집한다. 구현 완료 후 새로운 Claude 세션으로 고정된 base/head SHA를 읽기 전용 검토한다. 같은 맥락의 자기 검토는 독립 검토가 아니다.
- CLI 연결·실행 원칙은 docs/CLAUDE_WORKFLOW.md를 따른다. Claude는 CLAUDE.md와 AGENTS.md를 읽는다.
- 구현과 검토 프롬프트는 파일에 작성해 표준 입력으로 전달한다. CLI 출력·세션 ID는 git에서 제외한 .agent-runtime/에 보관한다.
- 실행 중인 동일 작업을 다시 실행하지 않는다. Codex exec 세션 ID나 확인 가능한 프로세스·Claude 세션 상태를 확인한다.
- 권한 우회 옵션은 사용하지 않는다. 파일 범위와 필요한 읽기·수정·검증 명령만 허용한다. 사용자 전역 설정을 변경하지 않는다.
- 검토 호출에는 파일 편집 도구를 주지 않는다. 문제를 찾으면 구현 담당 Claude 호출에서 수정하고 변경된 SHA를 다시 검토한다.

## 15분 협업 운영

- 총괄 자동화만 다음 작업을 배정한다. 담당 작업마다 별도 반복 자동화를 만들지 않는다.
- 매 실행 시 TASKS.md와 실제 작업 상태·Git 상태를 먼저 비교한다.
- 실행 중인 작업에는 반복 지시를 보내지 않는다. idle 상태만 보고 완료로 판단하지 않는다.
- 같은 작업 ID·같은 커밋을 중복 배정하거나 중복 통합하지 않는다.
- ready 작업 검토 → 수정 요청 또는 통합 → 의존성이 해결된 다음 작업 배정 순으로 진행한다.
- 한 실행에서 동일한 실패를 반복 재시도하지 않는다. 실패 원인과 재개 조건을 남긴다.
- 변경 없는 상태를 반복 보고하지 않는다. 의미 있는 완료·실패·사용자 결정이 필요한 경우에만 알린다.
- MVP 작업이 모두 완료되면 추가 기능을 자동 생성하지 않고 최종 결과를 보고한다. 이후 확인은 새 지시가 없으면 무변경으로 종료한다.
