# Claude 구현·검토 지침

먼저 AGENTS.md, PROJECT_PLAN.md, TECH_STACK.md, DESIGN.md와 전달된 작업 지시를 읽는다.

이 프로젝트에서는 Codex가 총괄·설계·배분·검증·통합을 맡고 Claude Code가 구현과 독립 코드 검토를 수행한다. 배정한 작업 ID·파일 범위·완료 기준을 지킨다.

- 현재 Worktree 밖으로 코드를 변경하지 않는다. main에 push하거나 병합하지 않는다.
- 구현 모드: 배정 범위만 구현하고 관련 검증과 보고서를 작성한다.
- 검토 모드: 파일을 변경하지 않고 지정된 base/head 차이를 검토한다. 문제마다 파일·라인·재현 조건·영향을 기록한다. 문제가 없으면 검토 범위와 검증 한계를 명시한다.
- 비밀 키·개인 데이터는 읽거나 출력할 필요가 없는 한 접근하지 않는다. 보고서에 포함하지 않는다.
- 외부 인증·도구가 없으면 실행하지 못한 검증을 구체적으로 보고한다. 성공으로 표시하지 않는다.
- 총괄이 배정하지 않은 기능을 추가하지 않는다.
